---
permalink: a5x/controller-replace-move-hardware.html
sidebar: sidebar
keywords: aff a5x, move the controller module hardware
summary: Replace the controller in your AFF A5X system when a hardware failure requires it. The replacement process involves removing the impaired controller, moving the components to the replacement controller, installing the replacement controller, and rebooting it.
---
= Replace the controller - AFF A5X
:icons: font
:imagesdir: ../media/

[.lead]
Replace the controller in your AFF A5X system when a hardware failure requires it. The replacement process involves removing the impaired controller, moving the components to the replacement controller, installing the replacement controller, and rebooting it.

include::../_include/a5x_controller_replace_move_hardware.adoc[]

.What's next?

After you've replaced the impaired AFF A5X controller, you need to link:controller-replace-system-config-restore-and-verify.html[restore the system configuration].